const terminal = () => {
    var div = document.querySelector("#terminal");
    div.classList.toggle("show");
}

const terminalFull = () => {
    var div = document.querySelector("#terminal");
    div.classList.toggle("full");
}